#include<stdio.h>
int main()
{
    int a;
    scanf("%d",&a);
    if (a%2==0)
    {
        printf(" %d is a even",a);
    }
    else
    {
        printf("%d is a odd",a);
    }
    
    return 0;
}